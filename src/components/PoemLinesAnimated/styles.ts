import * as React from "react";

export const poemBody: React.CSSProperties = {
    display: "flex",
    flexDirection: "column",
    marginLeft: "auto",
    marginRight: "auto",
    marginBottom: "10px",
};
